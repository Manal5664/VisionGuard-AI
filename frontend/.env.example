# Optional for direct local API calls. With no value, Vite proxies /api and /outputs locally.
VITE_API_BASE_URL=http://127.0.0.1:8000
# On Vercel, set this to the public Render service URL without a trailing slash.
# VITE_API_BASE_URL=https://visionguard-api.onrender.com
